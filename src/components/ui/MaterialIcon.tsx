type MaterialIconProps = {
  name: string;
  className?: string;
  label?: string;
};

export function MaterialIcon({ name, className = "", label }: MaterialIconProps) {
  void name;
  void className;
  void label;
  return null;
}
